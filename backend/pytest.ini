[pytest]
markers =
    unit: pure logic/service-layer tests with no HTTP client and no route wiring (fast, no TestClient).
    api: end-to-end tests that go through FastAPI's TestClient against real routes.
